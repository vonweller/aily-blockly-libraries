# ESP-NOW communication

ESP32 ESP-NOW wireless communication support library supports point-to-point communication, broadcast mode and network mode

## Library Info
- **Name**: @aily-project/lib-esp32-espnow
- **Version**: 1.0.2

## Block Definitions

| Block Type | Connection | Parameters (block.json order) | ABS Format | Generated Code |
|------------|------------|--------------------------|------------|----------------|
| `esp_now_master_init` | Statement | CHANNEL(input_value) | `esp_now_master_init(math_number(0))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ // 简化版广播Peer类 ↵ class ESP_NOW_Broadcast_Peer : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Broadcast_Peer(uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(ESP_NOW.BROADCAST_ADDR, channel, iface, lmk) {} ↵ ~ESP_NOW_Broadcast_Peer() { remove(); } ↵ bool begin() { ↵ if (!ESP_NOW.begin() &#124;&#124; !add()) { ↵ Serial.println("Failed to initialize broadcast peer"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ bool sendMessage(const String& msg) { ↵ return send((const uint8_t *)msg.c_str(), msg.length()); ↵ } ↵ }; ↵ ESP_NOW_Broadcast_Peer* esp_now_broadcast = nullptr; ↵ // ESP-NOW 主机模式初始化 ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA); ↵ Serial.println("ESP-NOW Master Mode"); ↵ Serial.println("MAC: " + WiFi.macAddress()); ↵ Serial.printf("Channel: %d\n", 1); ↵ esp_now_broadcast = new ESP_NOW_Broadcast_Peer(1); ↵ if (!esp_now_broadcast->begin()) { ↵ Serial.println("Failed to init ESP-NOW master"); ↵ delay(5000); ↵ ESP.restart(); ↵ }` |
| `esp_now_slave_init` | Statement | CHANNEL(input_value) | `esp_now_slave_init(math_number(0))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ String macToString(const uint8_t* mac) { ↵ char macStr[18]; ↵ snprintf(macStr, sizeof(macStr), "%02X:%02X:%02X:%02X:%02X:%02X", ↵ mac[0], mac[1], mac[2], mac[3], mac[4], mac[5]); ↵ return String(macStr); ↵ } ↵ String esp_now_rx_message; ↵ size_t esp_now_rx_len = 0; ↵ bool esp_now_rx_broadcast = false; ↵ String esp_now_rx_sender_mac; ↵ uint8_t esp_now_slave_channel = 1; ↵ void esp_now_on_message_handler() { ↵ // 用户回调将覆盖此函数 ↵ } ↵ // 简化版接收Peer类 ↵ class ESP_NOW_Receiver_Peer : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Receiver_Peer(const uint8_t *mac_addr, uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(mac_addr, channel, iface, lmk) {} ↵ ~ESP_NOW_Receiver_Peer() {} ↵ bool addPeer() { return add(); } ↵ bool sendMessage(const String& msg) { ↵ return send((const uint8_t *)msg.c_str(), msg.length()); ↵ } ↵ void onReceive(const uint8_t *data, size_t len, bool broadcast) { ↵ // 已注册设备的消息在此处理，转发到全局回调 ↵ esp_now_rx_sender_mac = macToString(addr()); ↵ esp_now_rx_message = String((char *)data, len); ↵ esp_now_rx_len = len; ↵ esp_now_rx_broadcast = broadcast; ↵ esp_now_on_message_handler(); ↵ } ↵ }; ↵ std::vector<ESP_NOW_Receiver_Peer*> esp_now_masters; ↵ void esp_now_register_new_master(const esp_now_recv_info_t *info, const uint8_t *data, int len, void *arg) { ↵ // 保存消息数据供用户回调使用 ↵ esp_now_rx_sender_mac = macToString(info->src_addr); ↵ esp_now_rx_message = String((char *)data, len); ↵ esp_now_rx_len = len; ↵ esp_now_rx_broadcast = (memcmp(info->des_addr, ESP_NOW.BROADCAST_ADDR, 6) == 0); ↵ // 检查是否为广播消息并注册新设备 ↵ if (esp_now_rx_broadcast) { ↵ // 检查是否已注册 ↵ for (auto& m : esp_now_masters) { ↵ if (memcmp(m->addr(), info->src_addr, 6) == 0) { ↵ esp_now_on_message_handler(); // 触发用户回调 ↵ return; ↵ } ↵ } ↵ // 注册新设备 ↵ Serial.printf("New master: " MACSTR "\n", MAC2STR(info->src_addr)); ↵ ESP_NOW_Receiver_Peer* new_master = new ESP_NOW_Receiver_Peer(info->src_addr, esp_now_slave_channel, WIFI_IF_STA, nullptr); ↵ if (new_master->addPeer()) { ↵ esp_now_masters.push_back(new_master); ↵ Serial.printf("Registered (total: %zu)\n", esp_now_masters.size()); ↵ } else { ↵ delete new_master; ↵ } ↵ } ↵ esp_now_on_message_handler(); // 触发用户回调 ↵ } ↵ // ESP-NOW 从机模式初始化 ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA); ↵ Serial.println("ESP-NOW Slave Mode"); ↵ Serial.println("MAC: " + WiFi.macAddress()); ↵ Serial.printf("Channel: %d\n", 1); ↵ if (!ESP_NOW.begin()) { ↵ Serial.println("Failed to init ESP-NOW"); ↵ delay(5000); ↵ ESP.restart(); ↵ } ↵ ESP_NOW.onNewPeer(esp_now_register_new_master, nullptr); ↵ Serial.println("Waiting for master...");` |
| `esp_now_broadcast_message` | Statement | MESSAGE(input_value) | `esp_now_broadcast_message(text("value"))` | `if (esp_now_broadcast != nullptr) { ↵ esp_now_broadcast->sendMessage("value"); ↵ }` |
| `esp_now_on_message_received` | Hat | HANDLER(input_statement) | `esp_now_on_message_received()` | `void esp_now_on_message_handler() { ↵ }` |
| `esp_now_received_message` | Value | (none) | `esp_now_received_message()` | `esp_now_rx_message` |
| `esp_now_received_message_len` | Value | (none) | `esp_now_received_message_len()` | `esp_now_rx_len` |
| `esp_now_received_is_broadcast` | Value | (none) | `esp_now_received_is_broadcast()` | `esp_now_rx_broadcast` |
| `esp_now_received_sender_mac` | Value | (none) | `esp_now_received_sender_mac()` | `esp_now_rx_sender_mac` |
| `esp_now_reply_message` | Statement | MESSAGE(input_value) | `esp_now_reply_message(text("value"))` | `espNowReplyMessage("value");` |
| `esp_now_node_init` | Statement | CHANNEL(input_value) | `esp_now_node_init(math_number(0))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ String macToString(const uint8_t* mac) { ↵ char macStr[18]; ↵ snprintf(macStr, sizeof(macStr), "%02X:%02X:%02X:%02X:%02X:%02X", ↵ mac[0], mac[1], mac[2], mac[3], mac[4], mac[5]); ↵ return String(macStr); ↵ } ↵ bool parseMacAddress(const String& macStr, uint8_t* mac) { ↵ if (sscanf(macStr.c_str(), "%hhx:%hhx:%hhx:%hhx:%hhx:%hhx", ↵ &mac[0], &mac[1], &mac[2], &mac[3], &mac[4], &mac[5]) != 6) { ↵ Serial.println("Invalid MAC address format"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ // 简化版广播Peer类 ↵ class ESP_NOW_Broadcast_Peer : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Broadcast_Peer(uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(ESP_NOW.BROADCAST_ADDR, channel, iface, lmk) {} ↵ ~ESP_NOW_Broadcast_Peer() { remove(); } ↵ bool begin() { ↵ if (!ESP_NOW.begin() &#124;&#124; !add()) { ↵ Serial.println("Failed to initialize broadcast peer"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ bool sendMessage(const String& msg) { ↵ return send((const uint8_t *)msg.c_str(), msg.length()); ↵ } ↵ }; ↵ String esp_now_rx_message; ↵ size_t esp_now_rx_len = 0; ↵ bool esp_now_rx_broadcast = false; ↵ String esp_now_rx_sender_mac; ↵ uint8_t esp_now_slave_channel = 1; ↵ ESP_NOW_Broadcast_Peer* esp_now_broadcast = nullptr; ↵ void esp_now_on_message_handler() { ↵ // 用户回调将覆盖此函数 ↵ } ↵ // 简化版接收Peer类 ↵ class ESP_NOW_Receiver_Peer : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Receiver_Peer(const uint8_t *mac_addr, uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(mac_addr, channel, iface, lmk) {} ↵ ~ESP_NOW_Receiver_Peer() {} ↵ bool addPeer() { return add(); } ↵ bool sendMessage(const String& msg) { ↵ return send((const uint8_t *)msg.c_str(), msg.length()); ↵ } ↵ void onReceive(const uint8_t *data, size_t len, bool broadcast) { ↵ // 已注册设备的消息在此处理，转发到全局回调 ↵ esp_now_rx_sender_mac = macToString(addr()); ↵ esp_now_rx_message = String((char *)data, len); ↵ esp_now_rx_len = len; ↵ esp_now_rx_broadcast = broadcast; ↵ esp_now_on_message_handler(); ↵ } ↵ }; ↵ std::vector<ESP_NOW_Receiver_Peer*> esp_now_masters; ↵ void esp_now_register_new_master(const esp_now_recv_info_t *info, const uint8_t *data, int len, void *arg) { ↵ esp_now_rx_sender_mac = macToString(info->src_addr); ↵ esp_now_rx_message = String((char *)data, len); ↵ esp_now_rx_len = len; ↵ esp_now_rx_broadcast = (memcmp(info->des_addr, ESP_NOW.BROADCAST_ADDR, 6) == 0); ↵ if (esp_now_rx_broadcast) { ↵ for (auto& m : esp_now_masters) { ↵ if (memcmp(m->addr(), info->src_addr, 6) == 0) { ↵ esp_now_on_message_handler(); ↵ return; ↵ } ↵ } ↵ Serial.printf("New peer: " MACSTR "\n", MAC2STR(info->src_addr)); ↵ ESP_NOW_Receiver_Peer* new_peer = new ESP_NOW_Receiver_Peer(info->src_addr, esp_now_slave_channel, WIFI_IF_STA, nullptr); ↵ if (new_peer->addPeer()) { ↵ esp_now_masters.push_back(new_peer); ↵ Serial.printf("Registered (total: %zu)\n", esp_now_masters.size()); ↵ } else { ↵ delete new_peer; ↵ } ↵ } ↵ esp_now_on_message_handler(); ↵ } ↵ // ESP-NOW 双向节点模式初始化 ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA); ↵ Serial.println("ESP-NOW Node Mode"); ↵ Serial.println("MAC: " + WiFi.macAddress()); ↵ Serial.printf("Channel: %d\n", 1); ↵ // 初始化广播peer ↵ esp_now_broadcast = new ESP_NOW_Broadcast_Peer(1); ↵ if (!esp_now_broadcast->begin()) { ↵ Serial.println("Failed to init ESP-NOW"); ↵ delay(5000); ↵ ESP.restart(); ↵ } ↵ // 注册新设备回调 ↵ ESP_NOW.onNewPeer(esp_now_register_new_master, nullptr); ↵ Serial.println("Ready for communication...");` |
| `esp_now_begin` | Statement | (none) | `esp_now_begin()` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowWaitWifiMode(WIFI_STA); ↵ if (!ESP_NOW.begin()) { ↵ Serial.println("Failed to initialize ESP-NOW"); ↵ ESP.restart(); ↵ }` |
| `esp_now_begin_with_pmk` | Statement | PMK(input_value) | `esp_now_begin_with_pmk(text("value"))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowWaitWifiMode(WIFI_STA); ↵ if (!ESP_NOW.begin((const uint8_t *)"value".c_str())) { ↵ Serial.println("Failed to initialize ESP-NOW"); ↵ ESP.restart(); ↵ }` |
| `esp_now_end` | Statement | (none) | `esp_now_end()` | `ESP_NOW.end();` |
| `esp_now_wifi_init` | Statement | MODE(dropdown), CHANNEL(input_value) | `esp_now_wifi_init(WIFI_STA, math_number(0))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA);` |
| `esp_now_create_peer` | Statement | VAR(field_input), MAC(input_value) | `esp_now_create_peer("peer1", text("value"))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowWaitWifiMode(WIFI_STA); ↵ if (!ESP_NOW.begin()) { ↵ Serial.println("Failed to initialize ESP-NOW"); ↵ ESP.restart(); ↵ } ↵ bool parseMacAddress(const String& macStr, uint8_t* mac) { ↵ if (sscanf(macStr.c_str(), "%hhx:%hhx:%hhx:%hhx:%hhx:%hhx", ↵ &mac[0], &mac[1], &mac[2], &mac[3], &mac[4], &mac[5]) != 6) { ↵ Serial.println("Invalid MAC address format"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ class ESP_NOW_Peer_peer1 : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Peer_peer1(const uint8_t *mac_addr, uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(mac_addr, channel, iface, lmk) {} ↵ String esp_now_data; ↵ size_t esp_now_len; ↵ bool esp_now_broadcast; ↵ bool esp_now_success; ↵ void onReceive(const uint8_t *data, size_t len, bool broadcast) { ↵ esp_now_data = String((char *)data, len); ↵ esp_now_len = len; ↵ esp_now_broadcast = broadcast; ↵ _onReceiveCallback(); ↵ } ↵ void onSent(bool success) { ↵ esp_now_success = success; ↵ _onSentCallback(); ↵ } ↵ virtual void _onReceiveCallback() {} ↵ virtual void _onSentCallback() {} ↵ }; ↵ uint8_t peer1_mac[6]; ↵ parseMacAddress("value", peer1_mac); ↵ ESP_NOW_Peer_peer1* peer1 = nullptr; ↵ peer1 = new ESP_NOW_Peer_peer1(peer1_mac); ↵ if (!peer1->add()) { ↵ Serial.println("Failed to register peer: peer1"); ↵ }` |
| `esp_now_create_peer_advanced` | Statement | VAR(field_input), MAC(input_value), CHANNEL(input_value), LMK(input_value) | `esp_now_create_peer_advanced("peer1", text("value"), math_number(0), text("value"))` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA); ↵ if (!ESP_NOW.begin()) { ↵ Serial.println("Failed to initialize ESP-NOW"); ↵ ESP.restart(); ↵ } ↵ bool parseMacAddress(const String& macStr, uint8_t* mac) { ↵ if (sscanf(macStr.c_str(), "%hhx:%hhx:%hhx:%hhx:%hhx:%hhx", ↵ &mac[0], &mac[1], &mac[2], &mac[3], &mac[4], &mac[5]) != 6) { ↵ Serial.println("Invalid MAC address format"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ class ESP_NOW_Peer_peer1 : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Peer_peer1(const uint8_t *mac_addr, uint8_t channel, wifi_interface_t iface, const uint8_t *lmk) ↵ : ESP_NOW_Peer(mac_addr, channel, iface, lmk) {} ↵ String esp_now_data; ↵ size_t esp_now_len; ↵ bool esp_now_broadcast; ↵ bool esp_now_success; ↵ void onReceive(const uint8_t *data, size_t len, bool broadcast) { ↵ esp_now_data = String((char *)data, len); ↵ esp_now_len = len; ↵ esp_now_broadcast = broadcast; ↵ _onReceiveCallback(); ↵ } ↵ void onSent(bool success) { ↵ esp_now_success = success; ↵ _onSentCallback(); ↵ } ↵ virtual void _onReceiveCallback() {} ↵ virtual void _onSentCallback() {} ↵ }; ↵ uint8_t peer1_mac[6]; ↵ parseMacAddress("value", peer1_mac); ↵ ESP_NOW_Peer_peer1* peer1 = nullptr; ↵ peer1 = new ESP_NOW_Peer_peer1(peer1_mac, 1, WIFI_IF_STA, (const uint8_t *)"value".c_str()); ↵ if (!peer1->add()) { ↵ Serial.println("Failed to register peer: peer1"); ↵ }` |
| `esp_now_create_broadcast_peer` | Statement | VAR(field_input) | `esp_now_create_broadcast_peer("broadcastPeer")` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ WiFi.mode(WIFI_STA); ↵ espNowWaitWifiMode(WIFI_STA); ↵ if (!ESP_NOW.begin()) { ↵ Serial.println("Failed to initialize ESP-NOW"); ↵ ESP.restart(); ↵ } ↵ // 简化版广播Peer类 ↵ class ESP_NOW_Broadcast_Peer : public ESP_NOW_Peer { ↵ public: ↵ ESP_NOW_Broadcast_Peer(uint8_t channel = 0, wifi_interface_t iface = WIFI_IF_STA, const uint8_t *lmk = nullptr) ↵ : ESP_NOW_Peer(ESP_NOW.BROADCAST_ADDR, channel, iface, lmk) {} ↵ ~ESP_NOW_Broadcast_Peer() { remove(); } ↵ bool begin() { ↵ if (!ESP_NOW.begin() &#124;&#124; !add()) { ↵ Serial.println("Failed to initialize broadcast peer"); ↵ return false; ↵ } ↵ return true; ↵ } ↵ bool sendMessage(const String& msg) { ↵ return send((const uint8_t *)msg.c_str(), msg.length()); ↵ } ↵ }; ↵ ESP_NOW_Broadcast_Peer* broadcastPeer = nullptr; ↵ broadcastPeer = new ESP_NOW_Broadcast_Peer(); ↵ if (!broadcastPeer->add()) { ↵ Serial.println("Failed to register broadcast peer: broadcastPeer"); ↵ }` |
| `esp_now_remove_peer` | Statement | VAR(field_variable) | `esp_now_remove_peer($peer1)` | `if (peer1 != nullptr) { ↵ ESP_NOW.removePeer(*peer1); ↵ delete peer1; ↵ peer1 = nullptr; ↵ }` |
| `esp_now_send` | Statement | VAR(field_variable), MESSAGE(input_value) | `esp_now_send($peer1, text("value"))` | `if (peer1 != nullptr) { ↵ peer1->send((const uint8_t *)"value".c_str(), "value".length()); ↵ }` |
| `esp_now_send_data` | Statement | VAR(field_variable), DATA(input_value), LEN(input_value) | `esp_now_send_data($peer1, math_number(0), math_number(0))` | `if (peer1 != nullptr) { ↵ peer1->send((const uint8_t *)&(1), 1); ↵ }` |
| `esp_now_on_receive` | Hat | VAR(field_variable), HANDLER(input_statement) | `esp_now_on_receive($peer1)` | `class ESP_NOW_Peer_peer1_WithCallback : public ESP_NOW_Peer_peer1 { ↵ public: ↵ using ESP_NOW_Peer_peer1::ESP_NOW_Peer_peer1; ↵ void _onReceiveCallback() override { ↵ } ↵ };` |
| `esp_now_on_sent` | Hat | VAR(field_variable), HANDLER(input_statement) | `esp_now_on_sent($peer1)` | `class ESP_NOW_Peer_peer1_WithSentCallback : public ESP_NOW_Peer_peer1 { ↵ public: ↵ using ESP_NOW_Peer_peer1::ESP_NOW_Peer_peer1; ↵ void _onSentCallback() override { ↵ } ↵ };` |
| `esp_now_on_new_peer` | Hat | HANDLER(input_statement) | `esp_now_on_new_peer()` | `String macToString(const uint8_t* mac) { ↵ char macStr[18]; ↵ snprintf(macStr, sizeof(macStr), "%02X:%02X:%02X:%02X:%02X:%02X", ↵ mac[0], mac[1], mac[2], mac[3], mac[4], mac[5]); ↵ return String(macStr); ↵ } ↵ String esp_now_src_mac_global; ↵ String esp_now_data_global; ↵ size_t esp_now_len_global; ↵ void esp_now_new_peer_callback(const esp_now_recv_info_t *info, const uint8_t *data, int len, void *arg) { ↵ esp_now_src_mac_global = macToString(info->src_addr); ↵ esp_now_data_global = String((char *)data, len); ↵ esp_now_len_global = len; ↵ } ↵ ESP_NOW.onNewPeer(esp_now_new_peer_callback, nullptr);` |
| `esp_now_received_data` | Value | (none) | `esp_now_received_data()` | `esp_now_data` |
| `esp_now_received_len` | Value | (none) | `esp_now_received_len()` | `esp_now_len` |
| `esp_now_is_broadcast` | Value | (none) | `esp_now_is_broadcast()` | `esp_now_broadcast` |
| `esp_now_send_success` | Value | (none) | `esp_now_send_success()` | `esp_now_success` |
| `esp_now_src_mac` | Value | (none) | `esp_now_src_mac()` | `esp_now_src_mac_global` |
| `esp_now_serial_create` | Statement | VAR(field_input), MAC(input_value), CHANNEL(input_value), MODE(dropdown) | `esp_now_serial_create("nowSerial", text("value"), math_number(0), WIFI_STA)` | `bool espNowWaitWifiMode(wifi_mode_t mode) { ↵ for (uint8_t i = 0; i < 50; i++) { ↵ if ((WiFi.getMode() & mode) == mode) { ↵ return true; ↵ } ↵ delay(20); ↵ } ↵ return (WiFi.getMode() & mode) == mode; ↵ } ↵ void espNowSetWifiChannel(uint8_t channel) { ↵ if (channel == 0) { ↵ return; ↵ } ↵ esp_err_t err = esp_wifi_set_channel(channel, WIFI_SECOND_CHAN_NONE); ↵ if (err != ESP_OK) { ↵ Serial.printf("esp_wifi_set_channel failed: 0x%x\n", err); ↵ } ↵ } ↵ const MacAddress peer_mac({0xvalue}); ↵ ESP_NOW_Serial_Class nowSerial(peer_mac, 1, WIFI_IF_STA); ↵ WiFi.mode(WIFI_STA); ↵ espNowSetWifiChannel(1); ↵ espNowWaitWifiMode(WIFI_STA); ↵ // ESP-NOW初始化 ↵ nowSerial.begin(115200);` |
| `esp_now_serial_available` | Value | VAR(field_variable) | `esp_now_serial_available($nowSerial)` | `nowSerial.available()` |
| `esp_now_serial_available_for_write` | Value | VAR(field_variable) | `esp_now_serial_available_for_write($nowSerial)` | `nowSerial.availableForWrite()` |
| `esp_now_serial_read` | Value | VAR(field_variable) | `esp_now_serial_read($nowSerial)` | `nowSerial.read()` |
| `esp_now_serial_read_string` | Value | VAR(field_variable) | `esp_now_serial_read_string($nowSerial)` | `nowSerial.readString()` |
| `esp_now_serial_read_string_until` | Value | VAR(field_variable), TERMINATOR(input_value) | `esp_now_serial_read_string_until($nowSerial, math_number(0))` | `nowSerial.readStringUntil('\n')` |
| `esp_now_serial_print` | Statement | VAR(field_variable), DATA(input_value) | `esp_now_serial_print($nowSerial, math_number(0))` | `nowSerial.print(1);` |
| `esp_now_serial_println` | Statement | VAR(field_variable), DATA(input_value) | `esp_now_serial_println($nowSerial, math_number(0))` | `nowSerial.println(1);` |
| `esp_now_serial_write` | Value | VAR(field_variable), DATA(input_value) | `esp_now_serial_write($nowSerial, math_number(0))` | `nowSerial.write(1)` |
| `esp_now_quick_broadcast` | Statement | MESSAGE(input_value), CHANNEL(input_value) | `esp_now_quick_broadcast(text("value"), math_number(0))` | `espNowQuickBroadcast("value", 1);` |
| `esp_now_quick_send` | Statement | MAC(input_value), MESSAGE(input_value), CHANNEL(input_value) | `esp_now_quick_send(text("value"), text("value"), math_number(0))` | `espNowQuickSend("value", "value", 1);` |
| `esp_now_get_mac` | Value | (none) | `esp_now_get_mac()` | `(WiFi.getMode() == WIFI_AP ? WiFi.softAPmacAddress() : WiFi.macAddress())` |
| `esp_now_get_max_data_len` | Value | (none) | `esp_now_get_max_data_len()` | `ESP_NOW.getMaxDataLen()` |
| `esp_now_get_peer_count` | Value | (none) | `esp_now_get_peer_count()` | `ESP_NOW.getTotalPeerCount()` |
| `esp_now_get_version` | Value | (none) | `esp_now_get_version()` | `ESP_NOW.getVersion()` |

## Parameter Options

| Parameter | Values | Description |
|-----------|--------|-------------|
| MODE | WIFI_STA, WIFI_AP, WIFI_AP_STA | esp_now_wifi_init |
| MODE | WIFI_STA, WIFI_AP | esp_now_serial_create |

## ABS Examples

### Basic Usage
```
arduino_setup()
    esp_now_master_init(math_number(0))
    serial_begin(Serial, 9600)

arduino_loop()
    serial_println(Serial, esp_now_received_message())
    time_delay(math_number(1000))
```

## Notes

1. **Variable**: `esp_now_create_peer("varName", ...)` creates variable `$varName`; pass `$varName` directly to `field_variable` slots; use `variables_get($varName)` only for `input_value` slots.
2. **Parameter order**: ABS parameters follow `block.json` args order.
3. **Input values**: use `math_number(n)`, `text("s")`, `logic_boolean(TRUE/FALSE)`, variables, or nested value blocks.
4. **Peer declaration order**: peer pointer declarations are emitted in the object section after the generated peer class definition, so create-peer blocks can be placed in setup without C++ type-order errors.
