{
  "schemaVersion": 1,
  "blocks": {
    "multistepper_positions_array": {
      "variadic": {
        "prefix": "INPUT",
        "startIndex": 1,
        "type": "input_value",
        "sampleCount": 1,
        "example": "math_number(200)",
        "reason": "The dynamic_inputs_mutator appends INPUT1, INPUT2, and later target-position inputs; named inputs preserve their identities during ABS import."
      }
    }
  }
}
