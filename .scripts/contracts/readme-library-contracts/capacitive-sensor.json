{
  "schemaVersion": 1,
  "blocks": {
    "capacitivesensor_wiring_hint": {
      "staticShape": true,
      "reason": "The extension resolves the existing non-serializable field_image media URL only; the hint block has no agent-facing ABS arguments and generates no code."
    }
  }
}
