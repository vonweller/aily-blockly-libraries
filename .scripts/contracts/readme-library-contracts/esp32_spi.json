{
  "schemaVersion": 1,
  "blocks": {
    "esp32_spi_create": { "agentVisible": false, "reason": "This legacy object-creation block is absent from the toolbox and its generator implementation is disabled." },
    "esp32_spi_begin": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_begin_custom": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_settings": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_begin_transaction": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_end_transaction": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_transfer": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_transfer16": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_write": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_write_bytes": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_set_frequency": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_set_bit_order": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_set_data_mode": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." },
    "esp32_spi_get_ss_pin": { "staticShape": true, "reason": "The custom-port hook only refreshes options for existing SPI instance fields." }
  }
}
