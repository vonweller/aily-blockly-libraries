{
  "schemaVersion": 1,
  "blocks": {
    "tftespi_setup": {
      "staticShape": true,
      "reason": "The tftespi_board_display_config extension only applies board defaults to existing fields; it does not append, remove, or reorder inputs."
    },
    "tftespi_play_animation": {
      "staticShape": true,
      "reason": "The playback extension only toggles visibility of the existing LOOP field; the serialized slot remains in its block.json position."
    }
  }
}
