{
  "schemaVersion": 1,
  "blocks": {
    "tftespi_setup": {
      "staticShape": true,
      "reason": "The tftespi_board_display_config extension applies board defaults and toggles the existing SPI/QSPI pin rows; it does not append, remove, or reorder inputs."
    },
    "tftespi_play_animation": {
      "staticShape": true,
      "reason": "The playback extension only toggles visibility of the existing LOOP field; the serialized slot remains in its block.json position."
    }
  }
}
